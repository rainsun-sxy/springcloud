spring:
  profiles:
    active:
      - dev         # 当前环境为测试环境
---

spring:
  profiles: dev     # 开发环境
  application:
    name: springcloud-config-dev
---

spring:
  profiles: test    # 测试环境
  application:
    name: springcloud-config-test

